package com.kevin.code.leet;

public class MaxProductSubArray {
    
    /*
     * Given an integer array nums, find the contiguous subarray
     * within an array (containing at least one number) which has
     * the largest product.
     */
    public int maxProduct(final int[] nums) {
        // Formulate as DP problem
        // DP(i, j) = max product of contiguous subarray from i (inc)
        // to j (exc)
        // TODO implement
        return -1;
    }

}
